# 内部服务器错误 - 故障排查指南

## 🔍 问题诊断步骤

当看到"内部服务器错误"提示时，请按以下步骤排查：

### 1. 查看浏览器控制台

**打开方式：**
- 按 `F12` 或 `Ctrl + Shift + I` 打开开发者工具
- 切换到 `Console`（控制台）标签

**查找信息：**
- 查找红色错误信息
- 查找以 `请求错误详情:` 开头的日志
- 查找以 `服务器错误详情:` 或 `服务器500错误详情:` 开头的日志

**示例日志格式：**
```javascript
请求错误详情: {
  url: "/dev-api/system/menu/getRouters",
  method: "get",
  status: 500,
  statusText: "Internal Server Error",
  data: { ... },
  message: "..."
}
```

### 2. 查看网络请求详情

**打开方式：**
- 在开发者工具中切换到 `Network`（网络）标签
- 刷新页面或重新触发错误

**检查内容：**
1. **请求状态码**
   - 红色状态码（如 500）表示服务器错误
   - 黄色状态码（如 401, 403）表示权限问题

2. **请求 URL**
   - 确认请求地址是否正确
   - 确认是否使用了正确的代理路径（`/dev-api`）

3. **请求头（Headers）**
   - 检查 `Authorization` 头是否存在
   - 检查 Token 格式是否正确（`Bearer {token}`）

4. **响应内容（Response）**
   - 查看服务器返回的具体错误信息
   - 复制错误信息用于排查

### 3. 常见错误原因及解决方案

#### 问题 1: JWT Token 格式错误

**错误信息：**
```
JWT strings must contain exactly 2 period characters. Found: 0
```

**原因：**
- Token 为空或格式不正确
- 未登录或登录失败

**解决方案：**
1. 清除浏览器 Cookie（特别是 `Admin-Token`）
2. 重新登录
3. 检查登录接口是否返回了有效的 `access_token`

#### 问题 2: 后端服务未启动

**错误信息：**
```
Network Error
后端接口连接异常
```

**原因：**
- 后端服务未启动
- 后端地址配置错误
- 网络连接问题

**解决方案：**
1. 确认后端服务已启动（检查后端控制台）
2. 检查 `vite.config.js` 中的代理配置：
   ```javascript
   proxy: {
     '/dev-api': {
       target: 'http://localhost:8080', // 确认这个地址正确
       changeOrigin: true,
       rewrite: (p) => p.replace(/^\/dev-api/, '')
     }
   }
   ```
3. 测试后端服务是否可访问：
   ```bash
   # 在浏览器中访问
   http://localhost:8080/actuator/health
   ```

#### 问题 3: 网关路由配置错误

**错误信息：**
```
404 Not Found
访问资源不存在
```

**原因：**
- 网关路由配置不正确
- 微服务未注册到 Nacos

**解决方案：**
1. 检查 Nacos 控制台，确认服务已注册
2. 检查网关路由配置
3. 确认微服务已启动

#### 问题 4: 数据库连接问题

**错误信息：**
```
数据库连接异常
SQL 执行错误
```

**原因：**
- 数据库服务未启动
- 数据库连接配置错误
- 数据库权限问题

**解决方案：**
1. 检查数据库服务是否运行
2. 检查数据库连接配置（`application.yml`）
3. 检查数据库用户权限

#### 问题 5: 权限不足

**错误信息：**
```
403 Forbidden
当前操作没有权限
```

**原因：**
- 用户角色权限不足
- Token 已过期

**解决方案：**
1. 检查用户角色和权限配置
2. 重新登录获取新 Token
3. 联系管理员分配权限

### 4. 快速检查清单

- [ ] 后端服务是否已启动？
- [ ] 网关服务是否正常运行？
- [ ] Nacos 服务是否正常运行？
- [ ] 数据库服务是否正常运行？
- [ ] 前端代理配置是否正确？
- [ ] 浏览器控制台是否有错误信息？
- [ ] 网络请求是否成功发送？
- [ ] Token 是否存在且有效？
- [ ] 后端日志是否有错误信息？

### 5. 获取详细错误信息

**方法一：查看浏览器控制台**
1. 打开开发者工具（F12）
2. 查看 Console 标签
3. 查找红色错误信息
4. 查看 `请求错误详情` 日志

**方法二：查看网络请求**
1. 打开开发者工具（F12）
2. 切换到 Network 标签
3. 找到失败的请求（红色）
4. 点击查看 Response 内容

**方法三：查看后端日志**
1. 查看后端控制台输出
2. 查看日志文件（通常在 `logs` 目录）
3. 查找异常堆栈信息

### 6. 调试技巧

#### 启用详细日志

在浏览器控制台执行：
```javascript
// 查看当前 Token
localStorage.getItem('Admin-Token') || document.cookie

// 查看环境变量
console.log('API地址:', import.meta.env.VITE_APP_BASE_API)
```

#### 测试后端连接

在浏览器中直接访问：
```
http://localhost:8080/actuator/health
```

如果返回 JSON 数据，说明后端服务正常。

#### 检查代理是否生效

在 Network 标签中查看请求：
- 请求 URL 应该是 `/dev-api/...`
- 实际请求应该转发到 `http://localhost:8080/...`

### 7. 常见配置检查

#### 检查环境变量

确认 `.env.development` 文件存在且配置正确：
```env
VITE_APP_BASE_API = '/dev-api'
```

#### 检查 Vite 配置

确认 `vite.config.js` 中的代理配置：
```javascript
proxy: {
  '/dev-api': {
    target: 'http://localhost:8080', // 后端地址
    changeOrigin: true,
    rewrite: (p) => p.replace(/^\/dev-api/, '')
  }
}
```

### 8. 联系支持

如果以上方法都无法解决问题，请提供以下信息：

1. **错误截图**
   - 浏览器控制台错误信息
   - Network 标签中的请求详情

2. **环境信息**
   - 操作系统版本
   - 浏览器版本
   - Node.js 版本
   - 后端服务版本

3. **错误日志**
   - 浏览器控制台完整错误日志
   - 后端服务错误日志

4. **复现步骤**
   - 详细描述操作步骤
   - 在什么情况下出现错误

## 📝 错误信息收集模板

```
【错误描述】
[在此描述你看到的错误]

【浏览器控制台错误】
[复制控制台的错误信息]

【网络请求详情】
- 请求 URL: 
- 请求方法: 
- 状态码: 
- 响应内容: 

【后端日志】
[如果有后端错误日志，请粘贴]

【环境信息】
- 操作系统: 
- 浏览器: 
- Node.js 版本: 
```

## 🔗 相关文档

- [后端对接指南](./后端对接指南.md)
- [JWT错误修复说明](./JWT错误修复说明.md)
