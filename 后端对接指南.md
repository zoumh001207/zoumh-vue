# 若依 Vue3 前端与后端对接指南

## 📋 目录
1. [环境配置](#环境配置)
2. [开发环境配置](#开发环境配置)
3. [生产环境配置](#生产环境配置)
4. [API 请求说明](#api-请求说明)
5. [常见问题](#常见问题)

---

## 🔧 环境配置

### 1. 环境变量文件说明

项目使用 Vite 的环境变量机制，支持以下环境文件：

- `.env.development` - 开发环境配置
- `.env.production` - 生产环境配置
- `.env.staging` - 测试环境配置

### 2. 关键配置项

| 配置项 | 说明 | 示例 |
|--------|------|------|
| `VITE_APP_BASE_API` | 后端 API 基础地址 | `/dev-api` 或 `http://localhost:8080` |
| `VITE_APP_TITLE` | 系统标题 | `若依管理系统` |
| `VITE_APP_ENV` | 环境标识 | `development` / `production` |

---

## 🛠️ 开发环境配置

### 方式一：使用 Vite 代理（推荐）

**适用场景：** 本地开发，后端运行在 `http://localhost:8080`

1. **配置 `.env.development`**
   ```env
   VITE_APP_BASE_API = '/dev-api'
   ```

2. **配置 `vite.config.js` 代理**
   ```javascript
   server: {
     port: 80,
     host: true,
     open: true,
     proxy: {
       '/dev-api': {
         target: 'http://localhost:8080',  // 后端服务地址
         changeOrigin: true,
         rewrite: (p) => p.replace(/^\/dev-api/, '')
       }
     }
   }
   ```

3. **工作原理**
   - 前端请求：`/dev-api/auth/login`
   - 代理转发：`http://localhost:8080/auth/login`
   - 自动处理跨域问题

### 方式二：直接配置后端地址

**适用场景：** 后端运行在其他地址或端口

1. **修改 `.env.development`**
   ```env
   VITE_APP_BASE_API = 'http://192.168.1.100:8080'
   ```

2. **注意跨域问题**
   - 如果后端未配置 CORS，需要在后端添加跨域支持
   - 或使用代理方式（推荐方式一）

---

## 🚀 生产环境配置

### 1. 配置生产环境地址

修改 `.env.production`：

```env
# 单机部署（前后端同域）
VITE_APP_BASE_API = '/prod-api'

# 或跨域部署（前后端不同域）
VITE_APP_BASE_API = 'https://api.yourdomain.com'
```

### 2. 构建生产版本

```bash
# 构建生产环境
npm run build:prod

# 或构建测试环境
npm run build:stage
```

### 3. 部署说明

#### 方案一：Nginx 反向代理（推荐）

```nginx
server {
    listen 80;
    server_name yourdomain.com;
    
    # 前端静态文件
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }
    
    # 后端 API 代理
    location /prod-api {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

#### 方案二：前后端分离部署

- 前端：部署到 CDN 或静态服务器
- 后端：独立部署，配置 CORS 允许前端域名访问

---

## 📡 API 请求说明

### 1. 请求拦截器配置

所有 API 请求都会经过 `src/utils/request.js` 处理：

- **自动添加 Token**：请求头自动添加 `Authorization: Bearer {token}`
- **统一错误处理**：自动处理 401、500 等错误
- **请求超时**：默认 10 秒超时

### 2. API 调用示例

```javascript
// src/api/login.js
import request from '@/utils/request'

// 登录接口
export function login(username, password, code, uuid) {
  return request({
    url: '/auth/login',
    headers: {
      isToken: false,        // 不需要 token
      repeatSubmit: false    // 不防重复提交
    },
    method: 'post',
    data: { username, password, code, uuid }
  })
}

// 获取用户信息
export function getInfo() {
  return request({
    url: '/system/user/getInfo',
    method: 'get'
    // 自动添加 token
  })
}
```

### 3. 后端接口规范

若依后端接口返回格式：

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    // 业务数据
  }
}
```

**状态码说明：**
- `200` - 成功
- `401` - 未授权，需要重新登录
- `403` - 无权限
- `500` - 服务器错误
- `601` - 警告

---

## 🔐 认证机制

### Token 管理

1. **Token 存储**：使用 `localStorage` 存储
2. **Token 格式**：`Bearer {token}`
3. **自动刷新**：401 错误时自动提示重新登录

### 登录流程

```javascript
// 1. 获取验证码
getCodeImg() → /code

// 2. 用户登录
login(username, password, code, uuid) → /auth/login

// 3. 获取用户信息
getInfo() → /system/user/getInfo

// 4. 获取菜单
getRouters() → /system/menu/getRouters
```

---

## ⚙️ 后端服务配置

### 若依微服务架构

根据 README.md，后端服务端口：

| 服务 | 端口 | 说明 |
|------|------|------|
| ruoyi-gateway | 8080 | 网关（前端主要对接此端口） |
| ruoyi-auth | 9200 | 认证中心 |
| ruoyi-system | 9201 | 系统模块 |
| ruoyi-gen | 9202 | 代码生成 |
| ruoyi-job | 9203 | 定时任务 |
| ruoyi-file | 9300 | 文件服务 |
| ruoyi-visual-monitor | 9100 | 监控中心 |

### 网关配置

前端统一通过网关（8080）访问后端服务，网关会自动路由到对应的微服务。

---

## ❓ 常见问题

### 1. 跨域问题

**问题：** 浏览器控制台报 CORS 错误

**解决方案：**
- 开发环境：使用 Vite 代理（推荐）
- 生产环境：后端配置 CORS 或使用 Nginx 反向代理

### 2. 401 未授权

**问题：** 接口返回 401 错误

**解决方案：**
- 检查 Token 是否过期
- 检查后端认证服务是否正常
- 检查 Token 格式是否正确（Bearer {token}）

### 3. 500 内部服务器错误

**问题：** 接口返回 500 错误

**解决方案：**
- 检查后端服务是否正常运行
- 查看后端日志排查具体错误
- 检查数据库连接是否正常

### 4. 验证码加载失败

**问题：** 登录页面验证码不显示

**解决方案：**
- 检查 `/code` 接口是否可访问
- 检查后端验证码服务是否启动
- 检查网络连接

### 5. 修改后端地址后不生效

**问题：** 修改环境变量后请求仍使用旧地址

**解决方案：**
- 重启开发服务器：`npm run dev`
- 清除浏览器缓存
- 检查环境变量文件是否正确

---

## 📝 快速开始

### 1. 启动后端服务

确保若依后端服务已启动，网关运行在 `http://localhost:8080`

### 2. 配置前端环境

```bash
# 开发环境（已配置好，无需修改）
# .env.development 中 VITE_APP_BASE_API = '/dev-api'

# 如需修改后端地址，编辑 vite.config.js 中的 proxy.target
```

### 3. 启动前端

```bash
npm install
npm run dev
```

### 4. 访问系统

打开浏览器访问：`http://localhost:80`

默认账号：`admin` / `admin123`

---

## 🔗 相关文档

- [若依官方文档](http://doc.ruoyi.vip)
- [Vite 官方文档](https://cn.vitejs.dev)
- [Axios 官方文档](https://axios-http.com)

---

## 💡 提示

1. **开发环境推荐使用代理**：避免跨域问题，配置简单
2. **生产环境使用 Nginx 代理**：性能好，配置灵活
3. **Token 管理**：系统已自动处理，无需手动管理
4. **错误处理**：系统已统一处理，会显示友好的错误提示

如有问题，请查看浏览器控制台和网络请求详情。
