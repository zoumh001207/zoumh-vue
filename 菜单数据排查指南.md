# 菜单数据排查指南

## 🔍 问题现象

系统登录成功，但侧边栏只显示"首页"，没有其他菜单项。

## 📋 排查步骤

### 1. 检查浏览器控制台

打开浏览器开发者工具（F12），查看 Console 标签：

**应该看到以下日志：**
```
后端返回的菜单数据: {...}
菜单数据类型: object true/false
处理后的路由数量: {...}
```

**如果看到：**
- `后端返回的菜单数据为空` - 说明后端返回了 null 或 undefined
- `后端返回的菜单数据为空数组` - 说明后端返回了空数组 `[]`
- `菜单数据为空，请检查后端菜单配置或用户权限` - 说明没有菜单数据

### 2. 检查网络请求

在 Network 标签中查找 `/system/menu/getRouters` 请求：

**检查内容：**
1. **请求状态码**
   - `200` - 请求成功
   - `500` - 服务器错误
   - `401` - 未授权
   - `403` - 无权限

2. **响应内容（Response）**
   - 查看返回的 JSON 数据
   - 检查 `data` 字段是否为数组
   - 检查数组是否为空

**正常响应格式：**
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": [
    {
      "name": "System",
      "path": "/system",
      "component": "Layout",
      "meta": {
        "title": "系统管理",
        "icon": "system"
      },
      "children": [...]
    }
  ]
}
```

**空数据响应：**
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": []
}
```

### 3. 后端排查

#### 检查数据库菜单数据

**SQL 查询：**
```sql
-- 查看所有菜单
SELECT * FROM sys_menu ORDER BY order_num;

-- 查看菜单数量
SELECT COUNT(*) FROM sys_menu;

-- 查看菜单树结构
SELECT menu_id, menu_name, parent_id, order_num, status 
FROM sys_menu 
WHERE status = '0' 
ORDER BY parent_id, order_num;
```

**可能的问题：**
- 数据库中没有菜单数据
- 菜单状态为禁用（`status = '1'`）
- 菜单类型不正确

#### 检查用户菜单权限

**SQL 查询：**
```sql
-- 查看当前用户的角色
SELECT r.role_id, r.role_name 
FROM sys_user_role ur
JOIN sys_role r ON ur.role_id = r.role_id
WHERE ur.user_id = 1;  -- 替换为实际用户ID

-- 查看角色分配的菜单
SELECT m.menu_id, m.menu_name, m.parent_id
FROM sys_role_menu rm
JOIN sys_menu m ON rm.menu_id = m.menu_id
WHERE rm.role_id = 1  -- 替换为实际角色ID
AND m.status = '0'
ORDER BY m.parent_id, m.order_num;
```

**可能的问题：**
- 用户没有分配角色
- 角色没有分配菜单权限
- 菜单权限配置不正确

#### 检查后端日志

查看后端控制台或日志文件，查找：
- `selectMenuTreeByUserId` 方法的执行日志
- SQL 查询日志
- 异常堆栈信息

### 4. 常见问题及解决方案

#### 问题 1: 数据库中没有菜单数据

**症状：** 后端返回空数组 `[]`

**解决方案：**
1. 执行数据库初始化脚本
2. 或手动插入菜单数据
3. 检查数据库连接配置

**初始化菜单 SQL（示例）：**
```sql
-- 插入系统管理菜单
INSERT INTO sys_menu (menu_name, parent_id, order_num, path, component, menu_type, visible, status, perms, icon, create_by, create_time)
VALUES ('系统管理', 0, 1, 'system', 'Layout', 'M', '0', '0', '', 'system', 'admin', NOW());

-- 插入用户管理菜单
INSERT INTO sys_menu (menu_name, parent_id, order_num, path, component, menu_type, visible, status, perms, icon, create_by, create_time)
VALUES ('用户管理', 1, 1, 'user', 'system/user/index', 'C', '0', '0', 'system:user:list', 'user', 'admin', NOW());
```

#### 问题 2: 用户没有菜单权限

**症状：** 后端返回空数组，但数据库有菜单数据

**解决方案：**
1. 检查用户角色分配
2. 检查角色菜单权限分配
3. 确保菜单状态为启用（`status = '0'`）

**分配菜单权限：**
```sql
-- 为角色分配菜单权限（示例：为角色ID=1分配菜单ID=1）
INSERT INTO sys_role_menu (role_id, menu_id)
VALUES (1, 1);
```

#### 问题 3: 菜单状态为禁用

**症状：** 数据库有菜单，但查询时被过滤

**解决方案：**
```sql
-- 启用所有菜单
UPDATE sys_menu SET status = '0' WHERE status = '1';
```

#### 问题 4: 菜单类型不正确

**症状：** 菜单数据存在，但前端不显示

**解决方案：**
- 确保菜单类型正确：
  - `M` - 目录
  - `C` - 菜单
  - `F` - 按钮

### 5. 快速测试

#### 测试后端接口

**使用 Postman 或 curl：**
```bash
# 获取 Token（先登录）
curl -X POST "http://localhost:8080/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123","code":"","uuid":""}'

# 使用 Token 获取菜单（替换 YOUR_TOKEN）
curl -X GET "http://localhost:8080/system/menu/getRouters" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

#### 测试数据库查询

在后端代码中添加日志：
```java
@GetMapping("getRouters")
public AjaxResult getRouters()
{
    Long userId = SecurityUtils.getUserId();
    System.out.println("当前用户ID: " + userId);
    
    List<SysMenu> menus = menuService.selectMenuTreeByUserId(userId);
    System.out.println("查询到的菜单数量: " + menus.size());
    
    List<SysMenu> builtMenus = menuService.buildMenus(menus);
    System.out.println("构建后的菜单数量: " + builtMenus.size());
    
    return success(builtMenus);
}
```

### 6. 前端调试

在浏览器控制台执行：
```javascript
// 查看当前菜单数据
console.log('侧边栏路由:', usePermissionStore().sidebarRouters)
console.log('所有路由:', usePermissionStore().routes)
console.log('添加的路由:', usePermissionStore().addRoutes)

// 手动触发获取菜单
usePermissionStore().generateRoutes().then(routes => {
  console.log('获取到的路由:', routes)
}).catch(err => {
  console.error('获取路由失败:', err)
})
```

### 7. 检查清单

- [ ] 数据库中有菜单数据
- [ ] 菜单状态为启用（status = '0'）
- [ ] 用户已分配角色
- [ ] 角色已分配菜单权限
- [ ] 后端接口返回 200 状态码
- [ ] 后端返回的 data 字段是数组
- [ ] 数组不为空
- [ ] 前端控制台没有错误
- [ ] 网络请求成功

## 🔧 临时解决方案

如果暂时无法解决，可以使用以下方法：

### 方法 1: 使用默认菜单

修改 `src/store/modules/permission.js`，在获取菜单失败时使用默认菜单：

```javascript
.catch(error => {
  console.error('获取路由失败:', error)
  // 使用默认路由
  const defaultMenus = [
    {
      path: '/system',
      component: 'Layout',
      children: [
        {
          path: 'user',
          component: 'system/user/index',
          name: 'User',
          meta: { title: '用户管理', icon: 'user' }
        }
      ]
    }
  ]
  this.setSidebarRouters(constantRoutes.concat(defaultMenus))
  resolve(defaultMenus)
})
```

### 方法 2: 直接访问菜单管理

如果知道菜单管理页面的路径，可以直接访问：
```
http://localhost:80/system/menu
```

## 📝 需要的信息

如果问题仍未解决，请提供：

1. **浏览器控制台日志**
   - 完整的 Console 输出
   - 特别是菜单相关的日志

2. **网络请求详情**
   - `/system/menu/getRouters` 的完整响应
   - 请求和响应头信息

3. **后端日志**
   - `selectMenuTreeByUserId` 的执行日志
   - SQL 查询日志
   - 任何异常信息

4. **数据库查询结果**
   - `SELECT * FROM sys_menu` 的结果
   - `SELECT * FROM sys_user_role WHERE user_id = ?` 的结果
   - `SELECT * FROM sys_role_menu WHERE role_id = ?` 的结果

## 🔗 相关文档

- [后端对接指南](./后端对接指南.md)
- [故障排查指南](./故障排查指南.md)
- [路由获取错误修复说明](./路由获取错误修复说明.md)
