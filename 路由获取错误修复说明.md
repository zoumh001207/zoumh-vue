# 路由获取错误修复说明

## 🔍 问题分析

根据控制台错误信息，问题出现在获取路由菜单时：

**错误信息：**
```
服务器500错误详情: {code: 500, msg: '内部服务器错误'}
Uncaught (in promise) Error: 内部服务器错误
```

**错误位置：**
- `request.js:109-110` - 响应拦截器
- `menu.js:5` - getRouters 函数
- `permission.js:38` - generateRoutes 方法

**问题原因：**
1. 后端 `/system/menu/getRouters` 接口返回 500 错误
2. 前端 `generateRoutes()` 方法没有 `.catch()` 处理错误
3. 导致未处理的 Promise 拒绝，显示 "Uncaught (in promise) Error"

## ✅ 修复内容

### 1. 修复 `permission.js` store 中的错误处理

**文件：** `src/store/modules/permission.js`

**修复前：**
```javascript
generateRoutes(roles) {
  return new Promise(resolve => {
    getRouters().then(res => {
      // ... 处理路由
      resolve(rewriteRoutes)
    })
    // ❌ 没有 .catch() 处理错误
  })
}
```

**修复后：**
```javascript
generateRoutes(roles) {
  return new Promise((resolve, reject) => {
    getRouters().then(res => {
      // ... 处理路由
      resolve(rewriteRoutes)
    }).catch(error => {
      // ✅ 捕获错误，避免未处理的 Promise 拒绝
      console.error('获取路由失败:', error)
      // 使用空路由，避免页面崩溃
      this.setRoutes([])
      this.setSidebarRouters(constantRoutes)
      this.setDefaultRoutes([])
      this.setTopbarRoutes([])
      reject(error)
    })
  })
}
```

### 2. 修复路由守卫中的错误处理

**文件：** `src/permission.js`

**修复前：**
```javascript
usePermissionStore().generateRoutes().then(accessRoutes => {
  // ... 处理路由
  next({ ...to, replace: true })
})
// ❌ 没有 .catch() 处理错误
```

**修复后：**
```javascript
usePermissionStore().generateRoutes().then(accessRoutes => {
  // ... 处理路由
  next({ ...to, replace: true })
}).catch(err => {
  // ✅ 捕获错误
  console.error('生成路由失败:', err)
  isRelogin.show = false
  // 如果是500错误，可能是后端问题，不强制退出登录
  if (err && err.message && err.message.includes('内部服务器错误')) {
    ElMessage.error('获取菜单路由失败，请稍后重试或联系管理员')
    next({ path: '/' })
  } else {
    // 其他错误（如401），退出登录
    useUserStore().logOut().then(() => {
      ElMessage.error(err || '获取用户信息失败')
      next({ path: '/login' })
    })
  }
})
```

## 🎯 修复效果

1. **避免未处理的 Promise 拒绝**
   - 所有错误都被正确捕获
   - 不再显示 "Uncaught (in promise) Error"

2. **更好的错误提示**
   - 500 错误时显示友好提示
   - 不会强制退出登录（可能是后端临时问题）

3. **页面不会崩溃**
   - 即使获取路由失败，也能继续使用基本功能
   - 使用默认路由，保证页面可访问

## 🔧 后端问题排查

虽然前端已经修复，但后端返回 500 错误仍需要解决。请检查：

### 1. 检查后端日志

查看后端控制台或日志文件，查找：
- `/system/menu/getRouters` 接口的错误信息
- 异常堆栈信息
- 数据库连接错误

### 2. 常见后端问题

#### 问题 1: 数据库连接失败
**症状：** 后端日志显示数据库连接异常

**解决方案：**
- 检查数据库服务是否运行
- 检查数据库连接配置
- 检查数据库用户权限

#### 问题 2: 服务未启动
**症状：** 网关无法找到 system 服务

**解决方案：**
- 确认 `ruoyi-system` 服务已启动
- 检查 Nacos 服务注册
- 检查网关路由配置

#### 问题 3: 权限验证失败
**症状：** JWT Token 验证失败

**解决方案：**
- 检查 Token 是否有效
- 检查 Token 格式
- 检查认证服务配置

#### 问题 4: SQL 执行错误
**症状：** 数据库查询失败

**解决方案：**
- 检查数据库表是否存在
- 检查 SQL 语句是否正确
- 检查数据库权限

### 3. 测试后端接口

可以直接测试后端接口：

```bash
# 使用 curl 测试（需要先登录获取 Token）
curl -X GET "http://localhost:8080/system/menu/getRouters" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

或在浏览器中：
1. 打开开发者工具
2. 切换到 Network 标签
3. 找到 `/system/menu/getRouters` 请求
4. 查看 Response 内容

## 📝 测试步骤

1. **清除浏览器缓存**
   - 清除 Cookie（特别是 `Admin-Token`）
   - 清除缓存

2. **重新登录**
   - 访问登录页面
   - 正常登录

3. **检查控制台**
   - 打开开发者工具（F12）
   - 查看 Console 标签
   - 确认不再有 "Uncaught (in promise) Error"

4. **检查网络请求**
   - 查看 Network 标签
   - 检查 `/system/menu/getRouters` 请求
   - 查看响应状态和内容

## ⚠️ 注意事项

1. **前端修复只是临时方案**
   - 前端错误处理已修复
   - 但后端 500 错误仍需解决
   - 建议尽快排查后端问题

2. **如果后端持续返回 500**
   - 检查后端服务状态
   - 查看后端日志
   - 联系后端开发人员

3. **临时解决方案**
   - 如果后端暂时无法修复
   - 前端会显示友好错误提示
   - 用户可以使用基本功能（使用默认路由）

## 🔗 相关文档

- [故障排查指南](./故障排查指南.md)
- [后端对接指南](./后端对接指南.md)
- [JWT错误修复说明](./JWT错误修复说明.md)
